# Profile recovery and provisional force-balance test

The first profile-export job reproduced the exact higher-output PROCESS result (relative net-power difference2.55e-14, identical radius, ifail1) but its exporter failed because the official physics object is a slotted data structure, not a `__dict__` object. The failed exporter and its successful solver check are retained in artifact10424080033/run35036578057. Admit one correction rerun using dataclass fields, not `vars()`. No physical equations, inputs, tolerances or performance targets change.

After successful profile extraction, admit at most two TokaMaker fixed-boundary solves at mesh spacings0.15m and0.075m, quadratic elements, one thread,120-second cap each, maximum100 nonlinear iterations. This is a provisional equilibrium-interface calculation, not a free-boundary divertor/coil solution or stability validation. The known fixed-boundary method and official API are reused.

Boundary: use the candidate R,a,kappa,triangularity to define the standard analytic contour, preserving physical triangularity via arcsin(delta). It has no X-point. Toroidal-field boundary constant is the candidate B0*R0; total current is the reproduced candidate Ip. Pressure shape is the reproduced thermal ne*Te plus ion contribution, with the candidate total stored-energy target if provided. Mapping the systems-model radius to sqrt(normalized poloidal flux), and assigning the same shape to unmodeled fast-particle pressure, are explicitly provisional choices. F*F' is the conventional decreasing linear profile; it is not fitted to obtain a desired efficiency or q result.

Record and compare the solved volume, magnetic-axis position, q profile/q95, stored energy and beta against the systems quantities. Preserve mismatch; do not relabel a numerically converged force-balanced proxy as the exact candidate. Require mesh refinement to preserve global quantities within1% before using its fields for an explicitly proxy-scoped ray calculation. Failure or inconsistent equilibrium fields stop wave transfer. Matching this finite-element test alone does not establish current evolution, control power or a physically achievable launcher.

This test addresses whether there is a usable, explicit equilibrium behind a wave calculation; it is not a new systems optimization, new fusion output, or transfer of the ITER canonical equilibrium to the candidate. A later ray calculation requires a separate named launch/model/power admission. The role of the30MW heating-only pool and required44.693kA/MW remain unqualified.
