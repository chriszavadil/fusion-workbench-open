# Research source snapshot

This directory preserves current project Python research code with source and transformed hashes in the parent manifest. Only contact-address text was removed from two modules. Imports and scientific calculations were not rewritten. Source syntax was checked; the full historical test suite was not rerun. Historical tests may require archived datasets or third-party packages not included here. Use the root approved-input worker for the tested current configuration reproductions.

Do not treat narrative numerical outputs as measured physical performance or combine results from incompatible configurations. Restricted data and private raw logs/correspondence are not published. Preserve upstream license requirements.
